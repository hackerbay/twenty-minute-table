{
  "name": "the-20-minute-table",
  "version": "1.0.0",
  "private": true,
  "description": "One hundred fast, whole-food recipes, typeset into a printable A4 cookbook and a static site.",
  "scripts": {
    "build": "python3 book/build.py && python3 book/render.py",
    "verify": "python3 book/verify.py"
  },
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.0.0",
    "@fontsource/inter": "^5.0.0"
  }
}
